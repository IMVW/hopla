<h1>Add employee</h1>

<%= simple_form_for(@user, method: :post, url: employees_path) do |f| %>
  <%= f.input :email %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :department_id, collection: @departments %>
  <%= f.submit 'New employee' %>
<% end %>
