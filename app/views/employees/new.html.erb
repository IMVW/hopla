
<%= simple_form_for(@employees, as: :employees, method: :post, url: employees_path) do |f| %>
  <%= f.input :email %>
  <%= f.input :last_name %>
  <%= f.input :first_name %>
  <%= f.submit 'New employees' %>
<% end %>
